import React from "react";
import Heder from "../../components/Header";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      <Heder />
    </div>
  );
};

export default Home;
